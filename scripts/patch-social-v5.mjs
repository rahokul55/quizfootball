import fs from 'node:fs/promises';let s=await fs.readFile('app-v5.js','utf8'),n=0;
if(!s.includes('function sendReaction(')){const anchor='function startPingLoop(){';const at=s.indexOf(anchor);if(at<0)throw new Error('social insertion anchor missing');const code=`function showReaction(name,emoji){const el=document.createElement("div");el.className="reaction-pop";el.innerHTML=\`<b>\${esc(name||"Oyuncu")}</b><span>\${emoji}</span>\`;document.body.appendChild(el);requestAnimationFrame(()=>el.classList.add("show"));setTimeout(()=>{el.classList.remove("show");setTimeout(()=>el.remove(),260)},1650)}
function sendReaction(emoji){const allowed=["🔥","👏","⚽","🤯","😂","👍"];if(!allowed.includes(emoji))return;const name=S.players[me]?.name||profile.username||"Oyuncu";showReaction(name,emoji);if(isHost)broadcast("reaction",{name,emoji});else send(hostConn,"reaction",{name,emoji})}
`;s=s.slice(0,at)+code+s.slice(at);n++}
if(!s.includes('m.type==="reaction"')&&s.includes('else if(m.type==="ping")send(c,"pong",{t:x.t})')){s=s.replace('else if(m.type==="ping")send(c,"pong",{t:x.t})','else if(m.type==="reaction"){const emoji=String(x.emoji||"");showReaction(S.players[c.peer]?.name||x.name,emoji);broadcast("reaction",{name:S.players[c.peer]?.name||x.name,emoji})}else if(m.type==="ping")send(c,"pong",{t:x.t})');n++}
if(!s.includes('hostMsgReactionV5')&&s.includes('else if(m.type==="result"){x.good?okSound():wrongSound()}')){s=s.replace('else if(m.type==="result"){x.good?okSound():wrongSound()}','else if(m.type==="reaction"){showReaction(x.name,x.emoji)}else if(m.type==="result"){x.good?okSound():wrongSound()}/*hostMsgReactionV5*/');n++}
if(!s.includes('id="reactionBar"')){const wire='function wire(){';const at=s.indexOf(wire);if(at<0)throw new Error('wire anchor missing');const pos=s.indexOf('{',at)+1;s=s.slice(0,pos)+'if(!document.getElementById("reactionBar")){const panel=document.querySelector(".activity-panel");if(panel){panel.insertAdjacentHTML("beforeend",`<div id="reactionBar" class="reaction-bar"><small>TEPKİ</small>${["🔥","👏","⚽","🤯","😂","👍"].map(e=>`<button type="button" data-react="${e}">${e}</button>`).join("")}</div>`);panel.querySelectorAll("[data-react]").forEach(b=>b.onclick=()=>sendReaction(b.dataset.react))}}'+s.slice(pos);n++}
if(!s.includes('window.QFSocial={react:sendReaction}')){s=s.replace('wire();\n})();','window.QFSocial={react:sendReaction};\nwire();\n})();');n++}
await fs.writeFile('app-v5.js',s);console.log(`Social V5 patches applied: ${n}`);if(!s.includes('function sendReaction(')||!s.includes('m.type==="reaction"')||!s.includes('reactionBar'))throw new Error('social reactions patch incomplete');
